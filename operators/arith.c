#include<stdio.h>
int main(){
   int a=10;
   int b=20;
    printf("addition of 2 nums a=%d,b=%d \n",a+b);
    printf("subtractin of 2 nums a=%d,b=%d\n",a-b);
    printf("multiplication of 2 nums a=%d,b=%d\n",a*b);
    printf("division of 2 nums a=%d,b=%d\n",a%b);
    printf("modulation of 2 nums a=%d,b=%d\n",a/b);
   // printf("pre increment a=%d\n",++a);
    printf("post increment a=%d\n",a++);
   // printf("pre decrement a=%d\n",--a);
    printf("post decrement a=%d\n",a--);
    return 0;
}
